version: '3.8'

services:
  angular-app:
    container_name: mi-angular-dev
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "4200:4200"
    volumes:
      - .:/app

      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true
